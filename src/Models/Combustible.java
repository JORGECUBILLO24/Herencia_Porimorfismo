package Models;

// Interfaz que define un contrato para los vehículos que usan combustible
public interface Combustible {
    // Método que deben implementar las clases que consumen combustible
    void recargar();
}
